<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_insert"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.asus.pbd_project2.Visitor.Plans.Insert">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="classification"
        android:text="cereals"
        android:background="@color/TitleBackground"
        android:id="@+id/classification"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/TitleBackground"
        android:id="@+id/name"
        android:hint="name"
        android:layout_below="@id/classification"
        android:layout_marginTop="30dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/TitleBackground"
        android:id="@+id/coloriesvalue"
        android:hint="coloriesvalue"
        android:layout_below="@id/name"
        android:layout_marginTop="30dp"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_below="@+id/coloriesvalue"
        android:layout_marginTop="40dp"
        android:text="Save"
        android:textAllCaps="false"
        android:textSize="20dp"
        android:id="@+id/savebtn"/>
</RelativeLayout>
